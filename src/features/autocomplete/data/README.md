# Korean Chosung Word Data

`chosung_words.json.gz` is derivative word-frequency data generated by the
Look-Talk project from `wordfreq` 3.1.1. It contains NFC-normalized Korean
words, their initial-consonant sequences, source frequencies, and filtered
frequency ranks. It is input data for Look-Talk's later Trie-building stage.

## Source and attribution

- Project: [`wordfreq`](https://github.com/rspeer/wordfreq)
- Author and required credit: **Robyn Speer**
- Version used: `3.1.1`
- Upstream notice: [`NOTICE.md`](https://github.com/rspeer/wordfreq/blob/master/NOTICE.md)
- Upstream code license: [Apache License 2.0](https://github.com/rspeer/wordfreq/blob/master/LICENSE.txt)
- Included word-frequency data and this derived dataset: [Creative Commons
  Attribution-ShareAlike 4.0](https://creativecommons.org/licenses/by-sa/4.0/)

The `wordfreq` code is licensed under Apache-2.0, while its included data files
contain material redistributable under CC BY-SA 4.0 and additional attribution
terms documented in the upstream `NOTICE.md`. The upstream notice identifies
sources and authors including Google Books Ngrams, the Leeds Internet Corpus,
Wikipedia, ParaCrawl, OPUS OpenSubtitles, and the SUBTLEX authors.

When redistributing `chosung_words.json.gz` or another dataset derived from it,
keep this README with the data, credit Robyn Speer by that name, preserve the
source and author attributions required by the upstream `NOTICE.md`, link the
applicable licenses, and apply the CC BY-SA 4.0 share-alike terms to the derived
data. Removing this attribution information does not satisfy those conditions.

## Look-Talk transformation

Look-Talk iterates the Korean `best` wordlist in its original descending
frequency order, normalizes each token to NFC, keeps only words composed
entirely of precomposed Hangul syllables (`U+AC00` through `U+D7A3`), preserves
one-syllable words, removes normalized duplicates after their first occurrence,
and adds the Chosung sequence and filtered rank. The deterministic build command
is:

```powershell
python -m scripts.build_chosung_dictionary
```

The generated JSON includes its source version, filtering rules, sorting rule,
license, and attribution. It contains no generation timestamp.

### Browser copy

The frontend copy is stored as uncompressed `chosung_words.json` because the
browser bundle does not read Python's gzip file at runtime. Its top-level
metadata and word order are preserved; only each word object is represented as
the compact tuple `[word, chosung, freq, rank]`. This is a transport-format
change, not a new dictionary or ranking transformation.

## Look-Talk hospital expressions

`hospital_phrases.csv` is a separate, manually curated Look-Talk dataset. It is
not used as the empty-input favorites list and is not derived from `wordfreq`.
It augments NLP autocomplete results only. The UTF-8 CSV columns are `text`,
`priority`, `category`, and `context`; Chosung sequences are computed at load
time instead of being stored in the file.

The loader NFC-normalizes each expression and accepts precomposed Hangul with
single spaces. It rejects leading, trailing, or repeated spaces, empty text,
Latin letters, numbers, punctuation, invalid contexts, and duplicate normalized
expressions. CSV order is the deterministic tie-breaker within a priority.
